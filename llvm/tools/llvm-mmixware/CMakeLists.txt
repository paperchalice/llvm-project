if(MMIX IN_LIST LLVM_TARGETS_TO_BUILD)
  add_subdirectory(llvm-mmixal)
  add_subdirectory(llvm-mmotype)
  add_subdirectory(llvm-mmix)
  add_subdirectory(llvm-mmmix)
else()
  set(LLVM_TOOL_LLVM_MMIXWARE_BUILD OFF CACHE BOOL "Whether to build llvm-mmixware as part of LLVM" FORCE)
endif()
